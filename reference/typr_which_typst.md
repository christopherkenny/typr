# Identify Install Typst to Use

Identify Install Typst to Use

## Usage

``` r
typr_which_typst()
```

## Value

a character vector or `NULL` if no Typst is found

## Examples

``` r
typr_which_typst()
#> NULL
```
